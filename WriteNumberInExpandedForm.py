def expandedForm(num):
    #convert num to string
    num = str(num)
    print(num)
    #get length of string
    #figure out place value for each item
    #add in the operation signs
    #retun completed string
